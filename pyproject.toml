[tool.poetry]
name = "telegram-chats-reader"
version = "0.1.0"
description = ""
authors = ["maxplatov <maxplatov@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
telethon = "^1.28.2"
pyyaml = "^6.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
